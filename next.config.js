/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['heygen.com', 'replicate.com', 'shotstack.io', 'imgbb.com'],
  },
}

module.exports = nextConfig
