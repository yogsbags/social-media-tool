# Social Media Frontend - Environment Variables
#
# NOTE: This frontend does NOT require API keys.
# All API keys are configured in the backend: ../projects/social-media/.env
#
# The frontend communicates with the backend via Node.js spawn,
# which runs the backend main.js script with all configured credentials.

# Backend Path (auto-detected, no need to change)
# BACKEND_ROOT=../

# Port Configuration (optional, defaults to 3004)
# PORT=3004
